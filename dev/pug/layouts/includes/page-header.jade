header.page-header
	- var indexLink = isStatic ? "index.html" : "<?php echo home_url(); ?>"
	a(href!= indexLink)
		h2.logo Любимая ферма

	- var pageTitle = isStatic ? title : "<?php wp_title(''); ?>"
	h1!= pageTitle
